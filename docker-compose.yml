version: '3.9'

services:
  # AlphaGalleon Backend API
  backend:
    build:
      context: alphagalleon-backend
      dockerfile: Dockerfile
    container_name: alphagalleon-backend
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - UPSTOX_ACCESS_TOKEN=${UPSTOX_ACCESS_TOKEN}
      - CONVEX_URL=${CONVEX_URL}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8000
      - ENVIRONMENT=production
    volumes:
      - ./alphagalleon-backend:/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # PostgreSQL Database (optional - for future use)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: alphagalleon-db
  #   environment:
  #     POSTGRES_USER: alphagalleon
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #     POSTGRES_DB: alphagalleon
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U alphagalleon"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Nginx Reverse Proxy (optional - for production)
  # nginx:
  #   image: nginx:alpine
  #   container_name: alphagalleon-nginx
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - backend
  #   restart: unless-stopped

networks:
  default:
    name: alphagalleon-network

# volumes:
#   postgres_data:
